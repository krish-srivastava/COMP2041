
Your `pigs` implementation can put anything it likes in this directory.

Your `.pig` repository doesn't need to contain a README file.

Your `.pig` repository should *NOT* contain the .reference directory,
this is just for the reference implementation.

